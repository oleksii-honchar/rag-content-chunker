# E2E test configuration for rag-content-chunker
# Points to local Mnemosyne MCP instance (started by mnemosyne-setup.ts)

watchSources:
  - id: e2e-test-source
    path: ./e2e-test-files
    include:
      - '*.md'
      - '*.ts'
      - '*.json'
      - '*.yaml'
      - '*.txt'
    exclude:
      - '**/node_modules/**'
    debounceMs: 500
    ignorePatterns: []

chunking:
  strategy: content-aware
  maxSizes:
    agentSessions: 400
    obsidianNotes: 500
    codeFiles: 400
    configuration: per-key
    plainText: 450
  overlap: 50
  hardCap: 600

enrichment:
  enabled: false

mcp:
  url: http://localhost:8765/sse
  timeoutMs: 5000
  maxRetries: 2
  retryDelayMs: 100

telemetry:
  enabled: false
